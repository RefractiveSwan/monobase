# Kanban – feature/fe-expansion (028)

**Theme:** Frontend expansion & UX deep dive for the mapping workbench  
**Branch:** `feature/app/FE-028-expansion`  
**Goal:** Turn the existing HTMX + Tailwind workbench into a richer client that surfaces more of the monorepo’s capabilities (analytics, dataset control, eval orchestration, observability) while keeping the view architecture clearly modularized.

> Status: **INPROGRESS**  
> Branch target version: `Unreleased`  
> Introduced in: `v0.1.0`  
> Last updated in: `v0.1.0`

---

## Background

The initial web workbench (REFR-027) reorganized `views/` but still exposes only a small slice of the pipeline: mapping entrypoints, metric snapshots, and eval toggles. The monorepo already ships capabilities that the UI could expose (analytics drilldowns, dataset store management, evaluation reports, NoMatch remediation, compliance insights, etc.). 028 pushes the frontend toward a “command center” that mirrors the CLI/API feature set.

---

## Columns

* **TODO** – Planned, not started
* **INPROGRESS** – Implementation/design happening
* **REVIEW** – Needs review or polish
* **DONE** – Completed & merged

---

## TODO

*Empty*

### 028-09 – API & configuration surfacing

- [x] Build an “Environment” page that enumerates the loaded env profiles (via `dfps_configuration::load_env` outcomes), highlighting overrides.  
- [x] Add a diagnostics endpoint in the frontend that proxies `/health` and `/metrics/summary`, showing raw JSON for debugging.  
- [x] Provide UI affordances to download API logs/no-match reports stored by `dfps_observability`.  
- [x] Display current backend URLs/config (API base, docs URL, GitHub URL) so operators know what profile they’re on.  
- [x] Wire feature flags (e.g., enable vector, enable mesh) through a centralized config widget tied to env or session storage.

---

## INPROGRESS

*Empty*

---

## REVIEW

### 028-01 – UI architecture & style system

- [x] Document the new `views/{components,layout,pages,partials,styles}` structure (README + diagrams).  
- [x] Extract shared Tailwind tokens / academic theme into a `styles/theme.rs` helper so pages don’t inline script blocks.  
- [x] Introduce a small Storybook-like preview route (`GET /ui/components`) that renders the reusable cards/buttons/badges for visual QA.  
- [x] Promote navbar/footer + announcements into a `layout::shell` that supports breadcrumbs and per-page callouts.  
- [x] Wire GitHub/Docs links centrally (navbar + footer) and keep the URLs in config/env for customization.

### 028-02 – Mapping workflow improvements

- [x] Add an “Upload history” panel (HTMX fragment) that lists the last N mapping jobs with status, duration, and link to results (requires server state caching).  
- [x] Provide inline validation summary (success/error badges) next to the paste/upload forms using the validation report returned by the API.  
- [x] Expand NoMatch explorer into a two-pane view: left = unresolved codes, right = remediation tips (anchors to CLI commands or docs).  
- [x] Support sample bundle templates (PET CT, oncology, blank) prefilled in the paste textarea.  
- [x] Allow downloading the latest mapping results as NDJSON directly from the UI.


### 028-03 – Analytics & observability surfacing

- [x] Build dedicated “Observability” page showing vector usage, compliance policy, dataset store health (pull from `/metrics/summary`).  
- [x] Extend analytics summary with filters (time range, state filters) and chart visualizations (HTMX fragments hitting `/analytics/*`).  
- [x] Surface cohort exports (download CSV) directly from the cohort view.  
- [x] Display compliance/licensing warnings (license-blocked counts, policy mode) prominently in the navbar or hero card.  
- [x] Add a live log panel (polling or SSE) for NoMatch reasons and API errors.

### 028-04 – Evaluation control center

- [x] Organize datasets by tier (bronze/silver/gold) with metadata pulled from `refractive_swan_eval`.  
- [x] Support scheduling eval runs (HTMX job queue + status badges) and show historical summary cards.  
- [x] Render calibration charts (precision/recall per code kind) using JSON generated by `eval_run`.  
- [x] Provide a “compare runs” view (diff two EvalSummary structs).  
- [ ] Expose dataset-store management actions (refresh fixtures, upload new manifest).

### 028-05 – Accessibility & documentation

- [x] Add accessibility checklist (ARIA labels, keyboard flows) for all forms/components.  
- [x] Create a frontend runbook (`docs/runbook/frontend-workbench.md`) covering setup, feature flags, and HTMX fragment tips.  
- [x] Provide screenshot-based docs (mdBook chapter) showing each page/state for release notes.  
- [x] Integrate Playwright or Cypress smoke tests for critical flows (mapping, analytics, eval).  
- [x] Document environment variables consumed by the frontend (GitHub link, docs URL, feature flags).

### 028-06 – Domain & platform surface area

- [x] Add a “Terminology Insights” panel that introspects `dfps_terminology` registries (licensed vs open systems, OBO graph cache health).  
- [x] Expose vector backend status (current backend, namespace, capacity snapshot) by calling into `dfps_vector_port` metrics.  
- [x] Provide a compliance policy viewer/editor reading from `dfps_compliance::Policy` so operators can see active rules without CLI.  
- [x] Surface ingestion validation counts (per `dfps_ingestion::validation::ValidationReport`) alongside mapping jobs.  
- [x] Allow operators to toggle lexical-only vs vector-backed mapping (propagate to API via query/config).

### 028-07 – Dataset & storage management

- [x] Build a “Dataset Store” admin page wired to `refractive_swan_eval::DatasetStore` for browsing, enabling/disabling datasets, and uploading new manifests.  
- [x] Provide download links for bundle/eval fixtures stored in `lib/domain/evaluation/fake_data/data` with provenance information.  
- [x] Add UI for triggering regression suite smoke tests (call `dfps_test_suite` runner) and displaying outcomes/history.  
- [x] Show warehouse/datamart connection status (backed by `dfps_datamart` health checks) and allow resetting the SQLite file via the API.  
- [x] Include a maintenance console for clearing HTMX fragment caches / session state.

### 028-08 – Mesh & future node readiness

- [x] Reserve a “Mesh” section that visualizes node capabilities using `dfps_mesh_dto::{MeshNodeId, NodeCapabilities}` once the node runtime exposes them.  
- [x] Add hooks for submitting mesh jobs (EvalDataset, AnalyticsQuery) to the upcoming `dfps_mesh_node` API and display responses in the UI.  
- [x] Provide governance policy previews (bridge to `dfps_mesh_governance`) so UI can show why a job was denied.  
- [x] Integrate vector store controls with mesh node context (per-node backend selection, capacity) when available.  
- [x] Document how mesh pages fall back gracefully when running in single-node/workbench mode.

---

## DONE

*Will be populated as tasks land.*

---

## Acceptance criteria

1. Frontend directory layout is documented and enforced (lint or dev docs).  
2. Mapping/workbench UI exposes history, validation status, and NoMatch remediation.  
3. Analytics/observability pages surface metrics, filters, and export options matching backend capabilities.  
4. Eval control center supports dataset browsing, run scheduling, and result comparisons.  
5. Accessibility, docs, and automated UI tests cover the new experiences.

---

## Risks & mitigations

- **Scope creep** – Many sub-features; use the kanban sections above to land incremental PRs (architecture → mapping → analytics → eval → docs).  
- **Backend gaps** – Some UI toggles may require new API endpoints; coordinate with API owners before exposing controls.  
- **HTMX fragmentation** – Ensure fragments cache/invalidate correctly; add integration tests where possible.  
- **Design consistency** – With more components, adopt Storybook route + design tokens to avoid drift.
