# refractive_swan_validation_port

Traits and DTOs for external validation (e.g., FHIR `$validate`).
Domain ingestion logic depends on these traits; platform/app crates implement
them when calling external services.
