[package]
name = "dfps_ingestion"
version.workspace = true
edition.workspace = true

[dependencies]
dfps_core = { path = "../core" }
dfps_fhir_profiles = { path = "../fhir_profiles", optional = true }
reqwest = { workspace = true, features = ["blocking"] }
serde.workspace = true
serde_json.workspace = true
thiserror = "1.0"

[features]
default = ["profile_validation"]
profile_validation = ["dfps_fhir_profiles"]
